let myName: string = 'Lotan';

let meaningOfLife: number;
let isLoading: Boolean;

let album: any;
let example: string | number;

meaningOfLife = 30;
isLoading = true;
album = 1948;

const sum = (a: number, b: string) => {
  return a + b;
};

let postId: string | number;
let isActive: number | boolean;

let re: RegExp = /\w+/g;
